# Git Rule

When creating commits:

- Do NOT add `Co-Authored-By` trailers
- Do NOT mention Anthropic, Claude, or any AI assistant in commit messages
- Keep commits clean — only the commit message, no AI attribution
